# UniConnect Import Guide

Use:
- `universities.json` for school selection
- `faculties.json` filtered by `university_id`
- `departments.json` filtered by `faculty_id`
- `courses.json` filtered by `department_id`

Recommended:
Build an Admin setup wizard where each school approves its exact faculties, departments, and programmes.
